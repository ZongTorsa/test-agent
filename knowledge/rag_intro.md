# RAG 学习笔记

RAG 是检索增强生成（Retrieval-Augmented Generation）。它先从知识库检索与问题相关的文本片段，再把这些片段作为上下文交给语言模型生成答案。

一个基础 RAG 流程包括：加载文档、文本切分、生成嵌入向量、写入向量数据库，以及根据用户问题进行相似度检索。

Chroma 是可以本地持久化的向量数据库，适合学习和小型项目。文本嵌入模型会将语义相近的句子映射到相近的向量位置，因此即使查询词与原文不完全相同，也能检索到相关内容。
